// Placeholder for the hospital administration overview dashboard.
// The real command-centre (KPI cards, forecast charts, roster, resource
// status, emergency map) is built in Phase 10.
export default function DashboardPage() {
  return (
    <div className="placeholder">
      <h2>Overview Dashboard</h2>
      <p>
        The hospital command centre — forecasts, resource status, staff roster,
        fairness indicators and emergency network — is built in Phase 10.
      </p>
    </div>
  )
}
